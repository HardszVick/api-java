spring:
  application:
    name: veiculos-api

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/veiculos}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

jwt:
  secret: ${JWT_SECRET:minha-chave-super-secreta-com-pelo-menos-32-caracteres}

server:
  port: 8085
